---
title: "sendToEverybody"
icon: "function"
description: "Broadcast a packet to all connected clients over the unreliable data channels. The server will frame the provided data with the configured magic terminator."
---

import { PageLink } from '/snippets/tsdocs/PageLink.jsx';
import { RefLink } from '/snippets/tsdocs/RefLink.jsx';

# sendToEverybody

## Summary

Broadcast a packet to all connected clients over the unreliable data channels. The server will frame the provided data with the configured magic terminator.  
## Signature

```typescript
sendToEverybody(data: Uint8Array): void;
```

## Parameters

### data
**Type**:`Uint8Array`

Raw packet bytes (Uint8Array) to send to every client

## Returns

**Type**:`void`

Broadcast a packet to all connected clients over the unreliable data channels. The server will frame the provided data with the configured magic terminator.