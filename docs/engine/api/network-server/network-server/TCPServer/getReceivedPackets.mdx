---
title: "getReceivedPackets"
icon: "function"
description: "Parse and return complete packets received from each client. Each packet is a Uint8Array buffer."
---

import { PageLink } from '/snippets/tsdocs/PageLink.jsx';
import { RefLink } from '/snippets/tsdocs/RefLink.jsx';

# getReceivedPackets

## Summary

Parse and return complete packets received from each client. Each packet is a `Uint8Array` buffer.  
## Signature

```typescript
getReceivedPackets(): Map<number, Uint8Array[]>;
```

## Parameters

*This method takes no parameters.*

## Returns

**Type**:`Map<number, Uint8Array[]>`

Parse and return complete packets received from each client. Each packet is a buffer.